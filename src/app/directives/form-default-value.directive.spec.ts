/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormDefaultValueDirective } from './form-default-value.directive';

describe('FormDefaultValueDirective', () => {
  it('should create an instance', () => {
    let directive = new FormDefaultValueDirective();
    expect(directive).toBeTruthy();
  });
});
